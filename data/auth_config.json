{
  "_doc": "Set api_base to your deployed Worker URL (cloudflare/SETUP.md). Empty = offline, no login screen, telemetry stays local. Web TTS is off unless the landing gate opens the build with a short-lived ?voice_token issued by /voice_token.",
  "api_base": "https://chalk-and-chance-api.jewoong-moon.workers.dev",
  "tts_requires_gate": true
}
